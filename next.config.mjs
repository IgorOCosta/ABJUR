/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    trailingSlash: true, // Garante que URLs funcionem como diretórios em páginas estáticas
    // outros ajustes que você possa precisar...
};

export default nextConfig;
